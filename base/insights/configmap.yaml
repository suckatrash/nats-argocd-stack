apiVersion: v1
kind: ConfigMap
metadata:
  name: insights-config
  namespace: insights
data:
  insights.yaml: |
    nats:
      url: nats://nats1.gcp.iamusingtheinternet.com:4222
      creds: /etc/insights/creds/sys.creds
    data-dir: /data
    scraper:
      interval: 1m
    log-level: info
