apiVersion: apps/v1
kind: Deployment
metadata:
  name: processor
  namespace: order-pipeline
  labels:
    app: processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: processor
  template:
    metadata:
      labels:
        app: processor
    spec:
      containers:
        - name: processor
          image: PLACEHOLDER
          command: ["/app/processor"]
          envFrom:
            - configMapRef:
                name: order-pipeline-config
          volumeMounts:
            - name: nats-creds
              mountPath: /etc/nats/creds
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: nats-creds
          secret:
            secretName: orders-nats-creds
